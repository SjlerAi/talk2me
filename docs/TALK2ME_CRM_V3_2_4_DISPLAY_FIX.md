# Talk2Me CRM v3.2.4 — display correction

- Fixed-service action buttons no longer wrap onto two lines.
- Mobile Edit Line and Open Line actions remain aligned on one line.
- The installed `package.json` release is now authoritative in the footer, so a stale cPanel `APP_VERSION` cannot display an older release.
- No database migration is required for this update.
