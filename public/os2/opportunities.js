(() => {
  const byId=id=>document.getElementById(id);
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  let view=null,type='upgrades',days=30,items=[];
  async function api(url,options={}){const r=await fetch(url,{...options,headers:{Accept:'application/json',...(options.headers||{})}});if(r.status===401){location.replace('/login');throw new Error('AUTHENTICATION_REQUIRED');}return r;}
  function notify(m){if(typeof window.toast==='function')return window.toast(m);alert(m);}
  function ensure(){if(view)return view;const s=document.createElement('style');s.textContent='.opp-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start;margin-bottom:20px}.opp-controls{display:flex;gap:8px;flex-wrap:wrap}.opp-controls button{border:1px solid var(--line);background:#fff;border-radius:10px;padding:10px 13px;font-weight:800;cursor:pointer}.opp-controls button.active{background:var(--blue);color:#fff}.opp-list{display:grid;gap:12px}.opp-card{background:#fff;border:1px solid var(--line);border-radius:16px;padding:16px;display:grid;grid-template-columns:1fr auto;gap:14px}.opp-meta{display:flex;gap:10px;flex-wrap:wrap;color:var(--muted);font-size:12px;margin:7px 0}.opp-actions{display:flex;gap:7px;flex-wrap:wrap;align-items:center}.opp-actions button,.opp-actions a{border:1px solid var(--line);background:#fff;border-radius:9px;padding:8px 10px;font-weight:800;text-decoration:none;color:inherit;cursor:pointer}.opp-actions .primary{background:var(--red);color:#fff;border-color:var(--red)}@media(max-width:760px){.opp-card{grid-template-columns:1fr}}';document.head.appendChild(s);view=document.createElement('section');view.className='content';view.id='opportunityView';view.hidden=true;view.innerHTML='<div class="opp-head"><div><span>SALES & CLIENT CARE</span><h1>Opportunities</h1><p id="oppStatus">Loading opportunities...</p></div><button class="secondary" id="oppRefresh">Refresh</button></div><div class="opp-controls" id="oppTypes"><button data-type="upgrades" class="active">Upgrades</button><button data-type="birthdays">Birthdays</button><button data-type="prospects">Prospects</button><button data-type="renewals">Renewals / cancellations</button></div><div class="opp-controls" id="oppDays" style="margin:10px 0 16px"><button data-days="0">Today</button><button data-days="7">7 days</button><button data-days="30" class="active">30 days</button><button data-days="60">60 days</button></div><div class="opp-list" id="oppList"></div>';document.querySelector('main').appendChild(view);byId('oppRefresh').onclick=load;byId('oppTypes').onclick=e=>{const b=e.target.closest('[data-type]');if(!b)return;type=b.dataset.type;byId('oppTypes').querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));load();};byId('oppDays').onclick=e=>{const b=e.target.closest('[data-days]');if(!b)return;days=Number(b.dataset.days);byId('oppDays').querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));load();};return view;}
  function hide(){['dashboardView','customerView','workView','approvalView','attendanceView'].forEach(id=>{const e=byId(id);if(e)e.hidden=true;});}
  function fmt(v){return v?new Intl.DateTimeFormat('en-ZA',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(v)):'—';}
  function render(){const list=byId('oppList');if(!items.length){list.innerHTML='<div class="panel" style="padding:30px;text-align:center">No matching opportunities.</div>';return;}list.innerHTML=items.map(i=>{const date=type==='birthdays'?i.birthday:type==='renewals'?i.cancellation_date:i.next_upgrade_date;const wa=String(i.cell_number||'').replace(/\D/g,'');return `<article class="opp-card"><div><strong>${esc(i.client_name)}</strong><div class="opp-meta"><span>${esc(i.account_number||'No account')}</span><span>${esc(i.assigned_staff)}</span><span>${esc(type)}: ${esc(fmt(date))}</span></div><p>${esc(i.package_name||'')} ${esc(i.handset||'')}</p></div><div class="opp-actions"><button data-client="${i.id}">Customer</button>${i.cell_number?`<a href="tel:${esc(i.cell_number)}">Call</a>`:''}${wa?`<a href="https://wa.me/${esc(wa)}" target="_blank">WhatsApp</a>`:''}<button data-contacted="${i.id}">Mark contacted</button><button class="primary" data-followup="${i.id}">Follow-up</button></div></article>`}).join('');list.querySelectorAll('[data-client]').forEach(b=>b.onclick=()=>window.openCustomer?.(Number(b.dataset.client)));list.querySelectorAll('[data-contacted]').forEach(b=>b.onclick=()=>contacted(b));list.querySelectorAll('[data-followup]').forEach(b=>b.onclick=()=>followup(b));}
  async function load(){ensure();byId('oppStatus').textContent='Loading opportunities...';try{const r=await api(`/api/opportunities?type=${encodeURIComponent(type)}&days=${days}`);const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'OPPORTUNITY_QUERY_FAILED');items=d.items||[];byId('oppStatus').textContent=`${d.count} opportunity${d.count===1?'':'ies'} · ${d.teamView?'Team view':'Your assigned clients'}`;render();}catch(e){byId('oppStatus').textContent=`Could not load opportunities: ${e.message}`;}}
  async function contacted(b){b.disabled=true;try{const r=await api(`/api/opportunities/${b.dataset.contacted}/contacted`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({method:'manual'})});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error);notify('Opportunity marked as contacted');}catch(e){notify(`Could not update: ${e.message}`);}finally{b.disabled=false;}}
  async function followup(b){const when=prompt('Follow-up date and time (YYYY-MM-DD HH:MM):');if(!when)return;const note=prompt('Follow-up note:')||'Opportunity follow-up';b.disabled=true;try{const r=await api(`/api/opportunities/${b.dataset.followup}/follow-up`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({followUpAt:when,note})});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error);notify('Follow-up added to My Work');window.loadDashboard?.();}catch(e){notify(`Could not schedule follow-up: ${e.message}`);}finally{b.disabled=false;}}
  async function show(){ensure();hide();view.hidden=false;document.querySelectorAll('.nav-item').forEach(n=>n.classList.toggle('active',n.dataset.view==='opportunities'));await load();}
  document.querySelectorAll('[data-view="opportunities"]').forEach(n=>n.addEventListener('click',e=>{e.preventDefault();show();}));window.showOpportunities=show;
})();
